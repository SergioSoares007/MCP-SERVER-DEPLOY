# Installation Steps

To install this mcp server, tun the following command

{
  "mcpServers": {
    "server": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/SergioSoares007/MCP-SERVER-DEPLOY.git",
		"mcp-server"
      ]
    }
    }
  }
